# LLM providers
anthropic>=0.40.0
openai>=1.50.0

# Async HTTP — used by all API adapters
httpx>=0.27.0

# AWS SES sending provider
boto3>=1.34.0

# Data models
pydantic>=2.0.0

# HTTP backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Redis async client — pipeline message bus between engines
redis>=5.0.0

# Agent 2: web scraping and research
firecrawl-py>=0.0.16

# Agent 3: email DB (sqlite3 is stdlib — no extra install needed)
