# LLM providers
anthropic>=0.40.0
openai>=1.50.0

# Async HTTP — used by all API adapters
httpx>=0.27.0

# Data models
pydantic>=2.0.0

# Redis async client — pipeline message bus between engines
redis[asyncio]>=5.0.0

# Agent 2: web scraping and news search
firecrawl-py>=0.0.16
serpapi>=0.1.5

# Agent 3: email DB (sqlite3 is stdlib — no extra install needed)
